language: ruby
rvm:
  - 2.1.0
install:
#  - sudo apt-get install texlive texlive-latex-extra
  - gem update --system # maybe fixes timeouts!?
  - gem install squib
script:
#  - pdflatex user_error.tex
  - ruby deck.rb
  - cp _output/phrases.pdf .
  - zip RunOn.zip phrases.pdf
addons:
  artifacts:
    paths:
    - RunOn.zip
